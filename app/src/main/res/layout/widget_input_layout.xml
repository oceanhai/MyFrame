<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/input_edit_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:paddingLeft="18dp"
    android:paddingRight="18dp"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/imageview_left"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>
    
    <EditText
        android:id="@+id/input_edit_text"
        android:singleLine="true"
        android:background="@null"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        style="@style/edit_text"/>

    <ImageView
        android:id="@+id/input_clear_button"
        android:visibility="gone"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/public_clear_btn_bg"
        android:layout_marginRight="12dp"/>
    
	<TextView
	    android:id="@+id/input_identifyingcode_button"
        android:visibility="gone"
        android:background="@drawable/circular_button_bg"
        android:gravity="center"
        android:padding="10dp"
        android:layout_marginRight="15dp"
        android:text="获取动态密码"
        android:textSize="13sp"
        android:textColor="@color/common_tv_color"
	    android:layout_width="wrap_content"
	    android:layout_height="wrap_content"/>
	
	<ImageView
        android:id="@+id/input_imageview_right"
        android:visibility="gone"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

</LinearLayout>